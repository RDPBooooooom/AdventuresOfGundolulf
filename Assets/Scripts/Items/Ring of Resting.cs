using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RingofResting : Item
{
    // Start is called before the first frame update
    void Start()
    {
        category = Category.Stats;
        value = 10;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
