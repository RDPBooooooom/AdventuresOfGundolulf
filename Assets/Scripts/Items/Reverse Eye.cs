using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ReverseEye : Item
{
    // Start is called before the first frame update
    void Start()
    {
        category = Category.Magic;
        value = 15;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
